// These are numbers with decimal points.
// For example: 1.0, 3.5, 7.8, 3.4 .....
// All floating points value are signed | can either be positive(+) or negative(-)

// They are of two types: f32 and f64

fn main() {
    // Examples
    let num: f32 = 2.3;
    let num_a: f64 = 100.45;
    let num_b: f32 = 12. ; // The . after ther number indicates it's a float

    // If type is not specified, f64 is default
    let num_c = 4.5; // type is f64
}